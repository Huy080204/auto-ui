# Backend Spring Boot (auto-ui-api), cổng 1000 theo application-local.properties
VITE_API_URL=http://localhost:1000
# Next.js — mở trang công khai sau khi publish + ping revalidate
VITE_WEB_URL=http://localhost:3000
# Page mặc định khi URL không có ?pageId=
VITE_PAGE_ID=1
